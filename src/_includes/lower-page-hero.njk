<header class="lower-visual-hero lower-visual-hero--{{ pageKey }}" aria-labelledby="{{ pageKey }}-page-title">
  <img src="{{ heroImage }}" alt="{{ heroImageAlt }}" fetchpriority="high">
  <div class="lower-visual-copy">
    <span>{{ heroKicker }}</span>
    <h1 id="{{ pageKey }}-page-title"><b>{{ heroTitleEn }}</b><small>{{ heroTitleJp }}</small></h1>
    <p>{{ heroLead }}</p>
  </div>
</header>
<nav class="page-breadcrumb" aria-label="パンくずリスト"><div class="site-shell"><a href="/">熊本・上通のパーソナルジム ZenNu</a><i>›</i><span>{{ breadcrumbLabel }}</span></div></nav>
